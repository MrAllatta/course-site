#lang racket

(require csv/reading)

(define next-row
  (make-csv-reader
   (open-input-file "lo.csv")
   '((separator-chars            #\,)
     (strip-leading-whitespace?  . #t)
     (strip-trailing-whitespace? . #t))))

(next-row)
